/**@jsx h */
/**@jsxFrag Fragment */

import { Fragment, h } from "$fresh/runtime.ts";
import { tw } from "$twind";

export default function Info() {
  return (
    <>
      <div>Some info about the app. How it works and so...</div>
    </>
  );
}
